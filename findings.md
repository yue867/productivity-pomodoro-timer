# 生活效率工具箱 - 第三次课最终迭代产品方案

## 一、工具清单与业务边界

### 现有工具（2个）
| 工具名称 | 业务边界 | localStorage Key |
|---------|---------|-----------------|
| 🍅 番茄钟 | 专注计时（25分钟）、短休息（5分钟）、长休息（15分钟）、番茄计数、自定义时长设置、提示音提醒 | `pomodoroSettings`, `pomodoroCount`, `pomodoroDate` |
| ✓ 待办事项 | 待办增删改查、状态切换、全部/待完成/已完成筛选、实时统计 | `todoItems` |

### 新增工具（第3个）
| 工具名称 | 业务边界 | localStorage Key |
|---------|---------|-----------------|
| 🔐 密码生成器 | 随机密码生成、密码长度自定义、字符类型选择（大写/小写/数字/特殊字符）、一键复制、历史记录 | `passwordSettings`, `passwordHistory` |

---

## 二、页面结构方案

### 导航栏结构
```
┌─────────────────────────────────────────────────────────────┐
│ 🍅 番茄钟    ✓ 待办事项    🔐 密码生成    👤 关于我         │
└─────────────────────────────────────────────────────────────┘
```

**Tab布局规则**：
- 前三个为工具Tab，按使用频率排序
- 最右侧为「关于我」简历Tab，与工具Tab有视觉分隔线

### 简历页内容字段规划
```
┌─────────────────────────────────────────────────────────────┐
│                    👤 个人简历                              │
├─────────────────────────────────────────────────────────────┤
│  📷 头像区域                                                │
│  姓名 | 职位                                                │
├─────────────────────────────────────────────────────────────┤
│  📝 个人简介                                                │
│  简短自我介绍、专业背景、核心优势                             │
├─────────────────────────────────────────────────────────────┤
│  💼 项目说明                                                │
│  本项目介绍、技术亮点、功能特点                             │
├─────────────────────────────────────────────────────────────┤
│  🛠️ 技术栈                                                  │
│  前端技术：HTML5, CSS3, JavaScript (ES6+)                  │
│  工具：GitHub, Git, VS Code                                │
│  设计：Soft UI, 响应式设计                                  │
├─────────────────────────────────────────────────────────────┤
│  📬 联系方式                                                │
│  GitHub链接、邮箱                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、数据层方案

### localStorage存储规范

| 工具 | 存储Key | 存储内容 |
|------|---------|---------|
| 番茄钟 | `pomodoroSettings` | { focusDuration, shortBreakDuration, longBreakDuration, tomatoesUntilLongBreak, soundEnabled } |
| 番茄钟 | `pomodoroCount` | 今日完成番茄数 |
| 番茄钟 | `pomodoroDate` | 记录日期（用于每日重置） |
| 待办事项 | `todoItems` | [{ id, text, completed, createdAt, completedAt }, ...] |
| 密码生成器 | `passwordSettings` | { length, uppercase, lowercase, numbers, symbols } |
| 密码生成器 | `passwordHistory` | [{ password, timestamp }, ...]（最近10条） |
| 全局设置 | `appSettings` | { theme: 'light' \| 'dark', lastUsedTool } |
| 使用统计 | `usageStats` | { pomodoroCount, todoAddCount, todoCompleteCount, passwordGenerateCount } |

### JSON全量导入/导出逻辑

**导出逻辑**：
1. 点击「导出数据」按钮
2. 遍历所有工具的localStorage数据
3. 打包为统一JSON格式
4. 自动下载为 `productivity-toolbox-backup-YYYYMMDD.json`

**导入逻辑**：
1. 点击「导入数据」按钮
2. 弹出文件选择框，选择JSON备份文件
3. 解析JSON数据
4. 逐条写入对应localStorage Key
5. 刷新页面或重新加载数据

**JSON数据格式**：
```json
{
  "exportDate": "2026-06-03T12:00:00.000Z",
  "version": "1.0.0",
  "data": {
    "pomodoroSettings": { ... },
    "pomodoroCount": 5,
    "pomodoroDate": "Tue Jun 03 2026",
    "todoItems": [...],
    "passwordSettings": { ... },
    "passwordHistory": [...],
    "appSettings": { ... },
    "usageStats": { ... }
  }
}
```

---

## 四、体验优化方案

### 统计面板

**统计维度**：
| 统计项 | 数据源 | 展示位置 |
|-------|-------|---------|
| 今日番茄完成数 | `pomodoroCount` | 番茄钟工具内 |
| 待办总数量 | `todoItems.length` | 待办工具内 |
| 待办完成率 | 已完成数/总数 | 待办工具内 |
| 密码生成次数 | `usageStats.passwordGenerateCount` | 密码工具内 |
| 各工具使用频次 | `usageStats` | 统计面板弹窗 |

**面板展示**：
- 入口：设置面板内「统计」按钮
- 展示方式：模态弹窗，卡片式布局
- 更新频率：实时更新

### 快捷操作区

**常用功能快捷按钮**：
| 快捷操作 | 目标工具 | 触发方式 |
|---------|---------|---------|
| 快速开始番茄 | 番茄钟 | 一键开始25分钟专注 |
| 添加待办 | 待办事项 | 快速输入弹窗 |
| 生成密码 | 密码生成器 | 一键生成默认密码 |

**最近使用记录**：
- 存储位置：`appSettings.recentTools`
- 存储内容：最近访问的3个工具ID列表
- 展示位置：导航栏下方快捷入口区

### 深浅色主题切换

**主题变量设计**：

| 变量 | 浅色模式 | 深色模式 |
|------|---------|---------|
| `--bg-color` | `#F0F4F8` | `#1A1A2E` |
| `--card-bg` | `#FFFFFF` | `#16213E` |
| `--text-primary` | `#2C3E50` | `#E8E8E8` |
| `--text-secondary` | `#5D6D7E` | `#A0A0A0` |
| `--primary-color` | `#4A90D9` | `#6BA3E0` |
| `--border-color` | `#E1E8ED` | `#2D3748` |

**切换逻辑**：
1. 点击设置面板中的「深色模式」开关
2. 切换`document.documentElement`上的`data-theme`属性
3. CSS通过`[data-theme="dark"]`选择器应用深色样式
4. 设置值持久化到`appSettings.theme`

---

## 五、UI规范

### 全局样式标准

**色彩体系**：
- 主色调：蓝色系（`#4A90D9`）用于按钮、选中状态
- 成功色：绿色系（`#5CB85C`）用于完成状态、成功提示
- 警告色：红色系（`#FF6B6B`）用于删除、错误提示
- 中性色：灰色系用于文本、边框

**间距规范**：
- 全局间距单位：8px
- 卡片内边距：16px
- 元素间距：8px ~ 16px

**圆角规范**：
- 小圆角（按钮、输入框）：8px
- 中圆角（卡片）：12px
- 大圆角（容器）：20px

**阴影规范**（Soft UI风格）：
```css
box-shadow: 8px 8px 16px rgba(174, 190, 205, 0.5),
            -8px -8px 16px rgba(255, 255, 255, 0.8);
```

---

## 六、验收用例清单

### 导航&简历页
| 用例ID | 测试项 | 预期结果 |
|--------|-------|---------|
| TC-001 | 导航栏4个Tab展示 | 番茄钟、待办事项、密码生成器、关于我依次展示 |
| TC-002 | Tab切换 | 点击任意Tab平滑切换，内容区域淡入淡出 |
| TC-003 | 简历页面展示 | 个人简介、项目说明、技术栈完整显示 |

### 工具功能
| 用例ID | 测试项 | 预期结果 |
|--------|-------|---------|
| TC-004 | 番茄钟计时 | 开始/暂停/重置功能正常，倒计时显示正确 |
| TC-005 | 待办事项管理 | 添加/完成/删除待办功能正常 |
| TC-006 | 密码生成器 | 生成密码、复制、历史记录功能正常 |

### 数据持久化
| 用例ID | 测试项 | 预期结果 |
|--------|-------|---------|
| TC-007 | 页面刷新数据留存 | 刷新后番茄计数、待办列表、密码历史不丢失 |
| TC-008 | JSON导出 | 导出文件包含所有工具数据 |
| TC-009 | JSON导入 | 导入备份文件后数据正确恢复 |

### 体验功能
| 用例ID | 测试项 | 预期结果 |
|--------|-------|---------|
| TC-010 | 统计面板 | 显示各工具使用频次和业务统计数据 |
| TC-011 | 快捷操作 | 快捷按钮可快速跳转到对应功能 |
| TC-012 | 深浅主题切换 | 点击开关全局样式切换，刷新后保持 |

### 兼容性
| 用例ID | 测试项 | 预期结果 |
|--------|-------|---------|
| TC-013 | 移动端适配 | 手机端布局自适应，触控操作流畅 |

### 项目资料
| 用例ID | 测试项 | 预期结果 |
|--------|-------|---------|
| TC-014 | README完整性 | 包含项目介绍、功能清单、在线地址、作者信息 |
| TC-015 | 代码规范 | 模块化拆分，关键逻辑有注释 |

---

## 七、工具矩阵总览

| 工具 | 功能模块 | 数据存储 | 核心特性 |
|------|---------|---------|---------|
| 🍅 番茄钟 | 专注计时、休息计时、番茄计数、设置管理 | pomodoroSettings, pomodoroCount, pomodoroDate | 25/5/15分钟循环、提示音、进度环 |
| ✓ 待办事项 | 添加、完成、删除、筛选、统计 | todoItems | 状态切换、筛选、完成率统计 |
| 🔐 密码生成器 | 密码生成、复制、历史记录、设置 | passwordSettings, passwordHistory | 自定义长度、字符类型选择 |
| 👤 关于我 | 个人简历展示 | 无（静态内容） | 个人信息、项目介绍、技术栈 |